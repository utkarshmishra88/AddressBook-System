# UC16 – Connect to MySQL Database

## Description
This use case connects the Address Book application to a MySQL database using JDBC.

A database named `addressbook_service` is used to store contact records.
