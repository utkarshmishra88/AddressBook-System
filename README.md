# UC5 – Add Multiple Contacts

## Description
This use case extends the application to support adding multiple contacts.

Contacts are stored using Java Collections (`ArrayList`) which allows managing multiple contact objects efficiently.
