# UC7 – Prevent Duplicate Contacts

## Description
This use case ensures that duplicate contacts cannot be added to the Address Book.

Duplicate detection is implemented using:
- `equals()` method
- Java Stream API
