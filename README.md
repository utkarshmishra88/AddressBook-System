# UC6 – Multiple Address Books

## Description
This use case introduces support for multiple address books.

A `Map` data structure is used where each address book is identified by a unique name.

Example:

Map<String, AddressBook>
